# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'QRCode_SwiftUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for QRCode_SwiftUI
  
  pod 'Alamofire', '~> 5.11'
  
  target 'QRCode_SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'QRCode_SwiftUIUITests' do
    # Pods for testing
  end

end
